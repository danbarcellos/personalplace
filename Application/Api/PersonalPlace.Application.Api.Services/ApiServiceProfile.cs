using AutoMapper;
using PersonalPlace.Application.Api.Services.Controllers.Catalog;
using PersonalPlace.Domain.Entities.Catalog;

namespace PersonalPlace.Application.Api.Services
{
    //public class ApiServiceProfile : Profile
    //{
    //    public ApiServiceProfile()
    //    {
    //        CreateMap<Realty, RealtyDTO>();
    //            //.ForMember(x => x.ClientName, opt => opt.MapFrom(x => x.Client.User.FirstName));
    //        CreateMap<Floorplan, FloorplanDTO>();
    //        CreateMap<Amenity, AmenityDTO>()
    //            .ForMember(x => x.AmenityTypeId, opt => opt.MapFrom(x => x.AmenityType.Id))
    //            .ForMember(x => x.AmenityTypeName, opt => opt.MapFrom(x => x.AmenityType.Name));
    //        CreateMap<Comment, CommentDTO>()
    //            .ForMember(x => x.MentionId, opt => opt.MapFrom(x => x.Mention.Id));

    //    }
    //}
}